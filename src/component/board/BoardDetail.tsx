import type { BoardResponse } from "../../type/Board";

const BoardDetail = ({ boardDetail }: { boardDetail: BoardResponse | null }) => {
    return (
        <span>143</span>
    )
}

export default BoardDetail;